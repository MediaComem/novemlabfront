extends layout

append scripts
    script(src='/javascripts/controllers/niveau5.js')

block content
    .main#niveau5(ng-controller="N5Controller as n5Ctrl")(ng-cloak)
        .row.question#niveau5
            .col.col-sm-8.center
                p#novemText
        .row.justify-content-center
            .col.col-sm-8.center.list-group
                a.list-group-item(href='#')(ng-repeat="prop in n5Ctrl.etape.propositions", value="{{prop.score}}") {{prop.reponse}}
            button.center.versNiveau(style='display:none') Étape suivante
